Place your legally obtained `SF Distant Galaxy.ttf` in this folder, then restart Vite or rebuild.
main.jsx discovers this exact optional filename and registers it with the FontFace API.
Only the display typography token uses it. Missing/invalid fonts safely use the system fallback.
No font is downloaded or included by default. Confirm licensing before distributing the font.
